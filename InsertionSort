def InsertionSort(l):
    n = len(l)
    if n<=1:
        return
    for i in range(1,n):
        key = l[i]
        j = i -1
        while j>=0 and l[j] > key:
            l[j+1] = l[j]
            j -=1
        l[j+1] = key
list1 = [27,12,64,1,16,34,2]
InsertionSort(list1)
print(list1)